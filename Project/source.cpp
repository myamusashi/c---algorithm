#include <iostream>
#include <regex>
#include <string>
#include <vector>
#include "login.h"

int main() {
	UserPass cekUser;
	cekUser.getID();
	std::regex pattern("^(?=.*\\d)(?=.*[a-zA-Z]).*$");
	if () {

	}
	return 0;
}
